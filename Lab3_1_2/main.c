#include <stdio.h>

int main() {
    FILE *input = fopen("input.txt", "r");
    FILE *output = fopen("output.txt", "w");
    int n, k = 0;
    int arr[1000];
    scanf("%x", &n);
    printf("%X", n);



}